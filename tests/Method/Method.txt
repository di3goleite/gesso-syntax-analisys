method int nomeDoMetodo (int a, float b, Class c) {
    a ++;
    c.atributo.metodo();
    a = b *2 + xk;
    
    if(x > "value" && (x == y || (y.y <= x && x == 5)) || X[0][0][0]){
        x = 1;
    } else {
        outro.metodo();
        contador++;
    }
    
    while(x+2>10){
        atualiza();
        proximo() + 10;
        x ++;
    }
    
    return true;
}
