if(x > "value" && (x == y || (y.y <= x && x == 5)) || X[0][0][0]){
    x = 1;
} else {
    outro.metodo();
    contador++;
    
    if(true || false){
        x = nested.cif();
        k --;
        m = vaaar + 10 * constante;
        metodo();
    }else{
        fazer.nada();
        nested = celse;
    }
}
